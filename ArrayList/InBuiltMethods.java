package ImportantQ.ArrayList;

public class InBuiltMethods {

    // .add(object) -> Adds object in end of list
    // .add(index, object) -> at specific Index
    // .addAll(Collection C) -> appends all elements from a specific DS
    // .addAll(index, Collection C) -> at index
    // .clear() -> Clear elements in ArrayList
    // .contains()
    // .get() -> returns element at specified Index
    // indexOf() -> returns -1 or specified element Index
    // .forEach() -> used to perform operation on all elements
        // Eg -  Numbers.forEach((n) -> System.out.println(n));
    // isEmpty()	Returns true if this list contains no elements.
    //lastIndexOf(Object O) -> returns last index of o
    //remove(int index) ->	Removes the element at the specified position in this list.
    //remove(Object o)	-> Removes the first occurrence of the specified element from this list, if it is present.
    // removeAll?(Collection c) -> 	Removes from this list all of its elements that are contained in the specified collection.

    // set(int index, E element) ->	Replaces the element at the specified position in this list with the specified element.

    // .trimToSize()	This method is used to trim the capacity of the instance of the ArrayList to the list’s current size.
    // .toArray()	This method is used to return an array containing all of the elements in the list in the correct order.
}
